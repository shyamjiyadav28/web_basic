# Saffron & Ember Restaurant Website

A responsive restaurant website for Hamirpur, Uttar Pradesh, with a React/Vite frontend, Express API, and local SQLite storage for reservations and orders.

## Run locally

```bash
npm install
npm run server
# in a second terminal
npm run dev
```

The frontend runs through Vite and proxies API calls to `http://localhost:3001`.

